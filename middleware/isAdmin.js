export default function ({ store, redirect }) {
  // if (process.browser) {
  //   // this.$nextTick(callback)
  //   // if (localStorage.getItem('wdc_token') !== null) {
  //   process.nextTick()
  //   // console.log(this.$el.textContent)
  //   if (store.getters['auth/getUserData'] !== null) {
  //     if (store.getters['auth/getUserData'].user.id_rol !== 1) {
  //       redirect('/dashboard')
  //     }
  //   }
  //   // }
  // }
  // process.nextTick(() => {
  //   setTimeout(() => {
  //     console.debug(store.getters['auth/getUserData'])
  //   }, 8000)
  // })
}
